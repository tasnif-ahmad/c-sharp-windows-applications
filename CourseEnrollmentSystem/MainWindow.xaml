<Window x:Class="CourseEnrollmentSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Course Enrollment System"
        Width="980"
        Height="560"
        WindowStartupLocation="CenterScreen"
        Background="#EEF2F7">

        <Window.Resources>

                <!-- SHADOW -->
                <DropShadowEffect x:Key="CardShadow"
                                  BlurRadius="18"
                                  ShadowDepth="4"
                                  Opacity="0.15"/>

                <!-- TEXTBOX -->
                <Style TargetType="TextBox">
                        <Setter Property="Height"
                                        Value="36"/>
                        <Setter Property="Padding"
                                        Value="10,6"/>
                        <Setter Property="BorderBrush"
                                        Value="#CBD5E1"/>
                        <Setter Property="BorderThickness"
                                        Value="1"/>
                        <Setter Property="FontSize"
                                        Value="14"/>
                        <Setter Property="Background"
                                        Value="White"/>

                        <Setter Property="Template">
                                <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                                <Border CornerRadius="8"
                                                        Background="{TemplateBinding Background}"
                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                        BorderThickness="{TemplateBinding BorderThickness}">

                                                        <!-- 👈 ROUNDNESS HERE -->
                                                        <ScrollViewer x:Name="PART_ContentHost"
                                                                      Margin="0"/>
                                                </Border>
                                        </ControlTemplate>
                                </Setter.Value>
                        </Setter>
                </Style>


                <!-- BUTTON -->
                <Style x:Key="ActionButton"
                       TargetType="Button">
                        <Setter Property="Height"
                                Value="32"/>
                        <Setter Property="FontSize"
                                Value="12"/>
                        <Setter Property="FontWeight"
                                Value="SemiBold"/>
                        <Setter Property="Foreground"
                                Value="White"/>
                        <Setter Property="Cursor"
                                Value="Hand"/>
                        <Setter Property="BorderThickness"
                                Value="0"/>
                        <Setter Property="Padding"
                                Value="6,2"/>

                        <Setter Property="Template">
                                <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="8">
                                                        <!-- 👈 ROUNDNESS HERE -->
                                                        <ContentPresenter
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                                </Border>
                                        </ControlTemplate>
                                </Setter.Value>
                        </Setter>
                </Style>


        </Window.Resources>

        <!-- MAIN LAYOUT -->

        <Grid Margin="24">
                <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Border Background="White"
                        CornerRadius="16"
                        Padding="20"
                        Margin="0,0,0,20"
                        Effect="{StaticResource CardShadow}">

                        <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🎓"
                                           FontSize="32"
                                           Margin="0,0,12,0"/>
                                <StackPanel>
                                        <TextBlock Text="Course Enrollment System"
                                                   FontSize="26"
                                                   FontWeight="Bold"
                                                   Foreground="#0F172A"/>
                                        <TextBlock Text="Manage courses with ease"
                                                   Foreground="#64748B"/>
                                </StackPanel>
                        </StackPanel>
                </Border>

                <!-- CONTENT -->
                <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="380"/>

                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- FORM CARD -->
                        <Border Grid.Column="0"
                                Background="White"
                                CornerRadius="16"
                                Padding="22"
                                Effect="{StaticResource CardShadow}">

                                <!-- ✅ FIXED FORM LAYOUT -->
                                <StackPanel>

                                        <TextBlock Text="Course Information"
                                                   FontSize="18"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,0,20"/>

                                        <TextBlock Text="Course Name"/>
                                        <TextBox x:Name="txtCourseName"
                                                 Margin="0,6,0,16"/>

                                        <TextBlock Text="Instructor"/>
                                        <TextBox x:Name="txtInstructor"
                                                 Margin="0,6,0,16"/>

                                        <TextBlock Text="Credits"/>
                                        <TextBox x:Name="txtCredits"
                                                 Margin="0,6,0,22"/>

                                        <!-- ✅ FIXED BUTTON LAYOUT -->
                                        <Grid Margin="0,0,0,2">
                                                <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <Button Grid.Column="0"
                                                        Content="Add"
                                                        Style="{StaticResource ActionButton}"
                                                        Background="#2563EB"
                                                        Margin="3"
                                                        Click="BtnAdd_Click"/>

                                                <Button Grid.Column="1"
                                                        Content="Update"
                                                        Style="{StaticResource ActionButton}"
                                                        Background="#059669"
                                                        Margin="3"
                                                        Click="BtnUpdate_Click"/>

                                                <Button Grid.Column="2"
                                                        Content="Delete"
                                                        Style="{StaticResource ActionButton}"
                                                        Background="#DC2626"
                                                        Margin="3"
                                                        Click="BtnDelete_Click"/>
                                        </Grid>


                                </StackPanel>
                        </Border>

                        <!-- TABLE CARD -->
                        <Border Grid.Column="2"
                                Background="White"
                                CornerRadius="16"
                                Padding="20"
                                Effect="{StaticResource CardShadow}">

                                <StackPanel>

                                        <TextBlock Text="Available Courses"
                                                   FontSize="18"
                                                   FontWeight="SemiBold"
                                                   Margin="0,0,0,14"/>

                                        <DataGrid x:Name="CoursesGrid"
                                                  AutoGenerateColumns="False"
                                                  CanUserAddRows="False"
                                                  GridLinesVisibility="None"
                                                  HeadersVisibility="Column"
                                                  RowHeight="38"
                                                  SelectionChanged="CoursesGrid_SelectionChanged">
                                                <!-- ✅ FIXED DATAGRID LAYOUT -->
                                                <DataGrid.Columns>
                                                        <DataGridTextColumn Header="ID"
                                                                            Binding="{Binding Id}"
                                                                            Width="60"/>
                                                        <DataGridTextColumn Header="Course"
                                                                            Binding="{Binding CourseName}"
                                                                            Width="*"/>
                                                        <DataGridTextColumn Header="Instructor"
                                                                            Binding="{Binding Instructor}"
                                                                            Width="*"/>
                                                        <DataGridTextColumn Header="Credits"
                                                                            Binding="{Binding Credits}"
                                                                            Width="80"/>
                                                </DataGrid.Columns>
                                        </DataGrid>

                                </StackPanel>
                        </Border>
                </Grid>
        </Grid>
</Window>
