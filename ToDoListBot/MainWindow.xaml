<Window x:Class="ToDoListBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoListBot"
        mc:Ignorable="d"
        Title="To-Do List" Height="450" Width="800">
    <Grid Margin="10">

        <!-- ROWS -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ADD TASK AREA -->
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,10">

            <TextBox x:Name="txtTask"
                     Width="400"
                     Height="35"
                     FontSize="16"
                     Margin="0,0,10,0"
                     VerticalContentAlignment="Center"/>

            <Button Content="Add Task"
                    Width="100"
                    Height="35"
                    Click="BtnAddTask_Click"/>
        </StackPanel>

        <!-- TASK LIST -->
        <ListBox x:Name="lstTasks"
                 Grid.Row="1"
                 FontSize="16"
                 Margin="0,10,0,0">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <CheckBox IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                              Checked="TaskStatusChanged"
                              Unchecked="TaskStatusChanged">

                        <TextBlock Text="{Binding Title}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="TextDecorations"
                                            Value="{x:Null}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCompleted}"
                                                     Value="True">
                                            <Setter Property="TextDecorations"
                                                    Value="Strikethrough"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                    </CheckBox>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

    </Grid>
</Window>
